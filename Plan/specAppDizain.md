# ALTER — Дизайн-спецификация приложения (specAppDizain.md)

> Перенос дизайн-системы сайта (specWEBDizain.md) на нативные мобильные паттерны + расположение элементов по экранам.
> v1.1 — исправлен конфликт нижней навигации: specChat.md §2.3 требует таб «Чаты», но версия v1 уже
> занимала все 5 слотов нижней навигации (Лента / Explore / +Создать / Уведомления / Профиль). Добавление
> «Чаты» как 6-го таба нарушает конвенции iOS HIG / Material (рекомендуемый максимум — 5 элементов, после
> этого либо ломается тап-зона на маленьких экранах, либо приходится урезать подписи). Исправлено в §3 ниже.

---

## 1. Направление

Та же тема — **«Backstage»**: студия+сцена, corner marks, дуотон magenta/amber. На приложении это должно читаться ещё плотнее к реальности — экран как «модель-лист» или «полароид с рабочего стола мейкера», а не как обычный соцсетевой UI с круглыми аватарками и мягкими карточками.

Ключевое отличие от веба: на телефоне меньше места — дуотон-градиенты используются реже и меньше (иначе съедают контент на маленьком экране), но **corner marks остаются** как главный узнаваемый элемент — они дешёвы по месту (тонкие штрихи по углам) и хорошо работают даже на маленьких превью.

---

## 2. Токены — перенос с адаптацией

### Цвет (без изменений, те же значения из specWEBDizain.md §2)
`--ink #12101A` · `--stage #1D1A29` · `--paper #F2EEE9` · `--magenta #E5487A` · `--amber #F2A93B` · `--line #3A3550`

Добавляется один токен, специфичный для приложения:
| Токен | HEX | Роль |
|---|---|---|
| `--stage-elevated` | `#252131` | Карточки поверх карточек (например, деталь заказа поверх kanban, или тред диалога поверх списка сообщений на планшетном layout) — на вебе такой глубины вложенности нет, на мобильном есть модалки поверх экранов |

Тёмная тема — единственная в MVP (см. specAPP.md §7 — документ пока не передан, см. примечание в §8), системная светлая тема не поддерживается.

### Типографика
Те же семейства (Syne / Space Grotesk / Space Mono), но шкала сжимается под мобильный экран:
| Роль | Веб | Приложение |
|---|---|---|
| Display (заголовок экрана) | 40–64px | 28–32px |
| Заголовок карточки | 20–28px | 18–20px |
| Body | 16px | 15–16px (минимум 15px — iOS/Android читаемость на маленьком экране) |
| Mono/utility (статусы, цены, дедлайны, таймстампы сообщений) | 12–14px | 12–13px |

### Touch-таргеты (специфика приложения, на вебе неактуально)
- Минимальный размер тап-зоны — 44×44pt (iOS HIG) / 48×48dp (Material) — применяется даже к мелким mono-бейджам статуса и к иконке-колокольчику уведомлений в шапке (см. §3), если они тапаются
- Расстояние между соседними тапаемыми элементами — не менее 8px, чтобы не было промахов пальцем

### Layout
- Без 12-колоночной сетки веба — мобильный layout: одна колонка с горизонтальными паддингами 16px (компактно) / 20px (просторно, на больших телефонах и в модалках)
- Скругления — то же правило, что на вебе: 4px на мелких элементах (бейджи, поля ввода), 0px на фото и карточках-«кадрах»
- Безопасные зоны (notch, home indicator, статус-бар) учитываются во всех экранах — контент не заходит под системные элементы

### Сигнатурный элемент — corner marks
Переносится как есть: L-образные штрихи по углам фото/карточек, magenta = активно/открыто/непрочитано, amber = дедлайн/ожидание. На маленьких превью (миниатюры в ленте, тач-скролл, аватары в списке диалогов) штрихи чуть толще (1.5px вместо 1px на вебе), чтобы не терялись при уменьшении.

---

## 3. Навигация (специфика приложения) — исправлено в v1.1

Нижняя таб-навигация, **5 табов, без исключений**:
```
[ Лента ]  [ Explore ]  [ + Создать ]  [ Чаты ]  [ Профиль ]
```
- «+ Создать» — не обычный таб, а выделенная кнопка по центру (билд/история), с ассиметричной формой (тот самый corner-mark мотив как акцент на кнопке)
- «Чаты» объединяет ЛС и тематические каналы в одном экране с переключателем сверху (как задано в specChat.md §2.3) — bottom-nav таб под это выделен, а не добавлен поверх существующих пяти
- **Уведомления перенесены из таб-бара в иконку-колокольчик в верхней шапке**, доступную на всех основных экранах (Лента/Explore/Чаты/Профиль). Обоснование переноса: уведомления — не основной режим использования (человек не «живёт» на этом экране так, как в Ленте или Чатах), в отличие от Чатов, которые по позиционированию продукта (specWEB.md §1, specChat.md §9) — одна из причин возвращаться. Бейдж-точка amber на колокольчике сохраняется при непрочитанных, deep link по тапу на пуш работает так же, как было бы с отдельным табом
- Если пользователь в роли мейкера — доступ к Maker Studio не отдельный таб, а свитчер сверху на табе «Профиль» (Профиль / Maker Studio), чтобы не плодить табы для роли, которая есть не у всех

---

## 4. Расположение по экранам

### 4.1 Онбординг
```
[ Экран 1: логотип ALTER, дуотон-фон (единственное место, где градиент занимает весь экран) ]
[ Экран 2: выбор роли(-ей) — карточки Косплеер / Мейкер / Фотограф, можно выбрать несколько ]
[ Экран 3: вход — email или Google/Apple ]
[ Экран 4 (опционально, можно пропустить): аватар + никнейм ]
```

### 4.2 Лента / Explore
```
[ Верх: поиск-строка (тап открывает полноэкранный поиск), иконка-колокольчик уведомлений справа от неё (см. §3) ]
[ Чипы фильтров — горизонтальный скролл, sticky под поиском ]
[ Подборки блоками, mono-заголовками: "НОВЫЕ БИЛДЫ" и т.д. ]
[ Внутри блока — горизонтальный скролл карточек-кадров ]
[ Pull-to-refresh на весь экран ]
```

### 4.3 Профиль (свой/чужой)
```
[ Header: аватар (corner marks), имя, роли-бейджи, статус коммишена крупным mono-лейблом ]
[ Ряд иконок-ссылок (Patreon/принт-шоп/соцсети) — горизонтальный скролл при большом количестве ]
[ Кнопка «Написать» рядом со статус-бейджем (чужой профиль) — открывает таб «Чаты» с этим диалогом ]
[ Таб-переключатель (внутриэкранный, не путать с нижней навигацией): Билды | Истории | О себе ]
[ Сетка билдов (2 колонки) или вертикальная лента историй ]
[ Плашка «Конвенты» ]
[ Если это чужой профиль с открытым коммишеном — sticky-кнопка снизу над таб-баром: «Запросить коммишен» ]
[ Если свой профиль и роль включает «мейкер» — свитчер вверху: Профиль / Maker Studio ]
```

### 4.4 Создание билда / истории
```
[ Шаг 1: выбор фото (камера/галерея, мультивыбор для билда) ]
[ Шаг 2: персонаж/франшиза (текстовое поле с автоподсказкой) ]
[ Шаг 3: теги соавторов — поиск по никнейму, добавление мейкера/фотографа ]
[ Шаг 4 (если есть теги): экран согласия — "Отправить запрос на подтверждение?" ]
[ Превью перед публикацией — карточка с corner marks, как она будет выглядеть в профиле ]
```

### 4.5 Запрос коммишена (wizard)
```
[ Полноэкранный wizard, прогресс-индикатор сверху (mono, "Шаг 2 из 5") ]
[ 1. Тип работы  2. Референсы (камера/галерея)  3. Замеры (шаблон-форма)
  4. Срок и бюджет  5. Условия — чекбокс подтверждения ]
[ Экран успеха: "Заявка №… отправлена", кнопки "Смотреть мои заявки" и "Написать мейкеру" (открывает автосозданный диалог в табе «Чаты») ]
```

### 4.6 Maker Studio (мобильная)
```
[ Верх: сводка mono-цифрами (в работе / просрочки / средний чек) — горизонтальный скролл если не помещается ]
[ Свайпаемые табы-колонки вместо kanban-доски: Новая · Обсуждение · В работе · Примерка · Готово · Отправлено ]
[ Карточка заказа внутри колонки: клиент, миниатюра, дедлайн (amber если близко), статус депозита ]
[ Свайп по карточке вправо = следующий статус (с подтверждением для необратимых переходов) ]
[ Тап по карточке → полноэкранная деталь: чек-лист (тапаемые чекбоксы), заметки, шаблон договора, кнопка "Отметить депозит оплаченным", кнопка "Открыть диалог с клиентом" ]
[ Отдельный таб внутри Maker Studio: Календарь загруженности ]
```

### 4.7 Уведомления (теперь модалка/полноэкранный экран из колокольчика, не таб — см. §3)
```
[ Список уведомлений, сгруппированный по дате (Сегодня / Ранее) ]
[ Каждое — иконка типа (заявка/депозит/дедлайн/согласие/новое сообщение), mono-таймстамп, тап открывает нужный экран (deep link) ]
[ Непрочитанные — точка amber слева ]
[ «← Назад» ведёт туда, откуда был открыт колокольчик — не на таб, которого больше нет ]
```

### 4.8 Чаты (новый таб — specChat.md §2, §3)
```
[ Верх экрана: переключатель «ЛС» / «Каналы» (сегмент-контрол, magenta на активном) ]
[ Строка поиска по диалогам/каналам под переключателем ]

Вкладка «ЛС»:
[ Список диалогов: квадратный аватар с corner marks (magenta corner = непрочитано),
  никнейм, превью последнего сообщения (обрезано), mono-таймстамп справа,
  закреплённые — вверху списка с amber-меткой закрепа ]
[ Тап по диалогу → полноэкранный тред (отдельный экран, не модалка):
  шапка — аватар+имя+«печатает…», кнопки «Пожаловаться»/«Заблокировать» в меню шапки (три точки),
  лента сообщений, поле ввода снизу с кнопкой вложения фото (без видео в MVP),
  офлайн — исходящие уходят в очередь, индикатор «отправка…» на самом сообщении (specAPP.md §2 — офлайн-паттерн, документ не передан, см. примечание §8 ниже) ]

Вкладка «Каналы»:
[ Список каналов, сгруппированный по типу (mono-заголовки секций): Франшиза / Конвент / Город ]
[ Карточка канала: иконка/обложка с corner marks, название, число участников mono ]
[ Тап → тред канала: закреплённое сообщение сверху, кнопка «Участники» в шапке открывает лист участников
  с переходом в профиль по тапу; модератору доступны свайп-действия «Закрепить»/«Удалить»/«Забанить» на сообщении ]

Пустое состояние (specStates.md, применяется и здесь):
[ «Пока нет диалогов» + CTA «Найти мейкера/косплеера» → таб Explore ]
```

---

## 5. Компоненты (сквозные, специфика приложения)

- **Нижний таб-бар**: stage-фон, активный таб — иконка + подпись magenta, неактивные — paper с пониженной непрозрачностью; 5 табов фиксированно (см. §3)
- **Иконка-колокольчик уведомлений**: в верхней шапке, amber-точка при непрочитанных, тап открывает полноэкранный список (§4.7) — заменяет собой отдельный таб
- **Статус-бейдж**: тот же, что на вебе (mono, uppercase) — `ОТКРЫТО` / `ЗАКРЫТО` / `ЛИСТ ОЖИДАНИЯ`, но с увеличенной тап-зоной, если кликабелен
- **Карточка-кадр**: то же визуальное решение, что на вебе, адаптированная под 2-колоночную сетку в профиле и горизонтальный скролл в лентах
- **Аватар с corner marks** (список диалогов, участники канала): квадрат, не круг — тот же язык, что у карточки-кадра (specWEBDizain.md §2)
- **Свайп-действие** (только приложение): фон-подложка под карточкой при свайпе показывает следующий статус mono-текстом до отпускания пальца; в Чатах — свайп по диалогу открывает «Заблокировать»/«Архивировать»
- **Wizard-прогресс**: тонкая линия сверху экрана, mono подпись шага — переиспользуется в создании билда и запросе коммишена
- **Пустые состояния**: иллюстрация в стиле corner marks (рамка без содержимого) + короткий mono-текст + primary-кнопка действия

---

## 6. Доступность и адаптивность

- Динамический размер шрифта системы (iOS Dynamic Type / Android font scale) — учитывается, layout не ломается при увеличенном шрифте
- Контраст — то же требование AA, что на вебе
- `prefers-reduced-motion` эквивалент — уважаем системную настройку «уменьшить движение», свайпы/переходы без лишней анимации при этом флаге
- Все интерактивные элементы достижимы для VoiceOver/TalkBack (подписи, не только иконки) — включая колокольчик уведомлений и переключатель ЛС/Каналы
- Безопасные зоны устройства (см. §2) — обязательны на каждом экране

---

## 7. Дальше по плану

1. ✅ specWEB.md
2. ✅ specWEBDizain.md
3. ✅ Визуальный дизайн сайта
4. ⏳ specAPP.md — **не найден в переданных файлах.** specChat.md §2.3, §6 и этот документ (§2, §4.8) прямо ссылаются на его содержимое (офлайн-очередь сообщений, детали стека приложения) — пришли его, если он существует, иначе эти ссылки помечены выше и нуждаются в отдельном согласовании
5. ✅ specAppDizain.md — этот документ (v1.1, навигация исправлена, добавлены экраны Чатов)
6. ⏳ Визуальный дизайн приложения (`app-mockups.html`, упомянутый в specChat.md §10 — тоже не найден в переданных файлах)
